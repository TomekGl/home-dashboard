apiVersion: v1
kind: Service
metadata:
  name: home-dashboard
  namespace: home-dashboard
  labels:
    app: home-dashboard
spec:
  type: ClusterIP
  selector:
    app: home-dashboard
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
